<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Register Gift 🎁</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    class="min-h-screen bg-gradient-to-br from-yellow-100 via-white to-pink-100 flex items-center justify-center"
  >
    <div class="w-full max-w-md bg-white rounded-3xl shadow-xl p-8">
      <h1 class="text-2xl font-bold text-pink-600 text-center mb-4">
        🎁 Register Your Gift
      </h1>

      <% if (typeof lastGift !== "undefined" && lastGift !== null) { %>
      <p class="text-sm text-gray-600 text-center mb-4">
        Last registered gift number:
        <span class="font-bold text-pink-600"><%= lastGift %></span>
        — you can continue with
        <span class="font-bold text-emerald-600"><%= lastGift + 1 %></span>
      </p>
      <% } else { %>
      <p class="text-sm text-gray-600 text-center mb-4">
        No gifts registered yet. Start with gift number
        <span class="font-bold text-emerald-600">1</span>.
      </p>
      <% } %>

      <% if (error) { %>
      <div
        class="mb-4 rounded-2xl border border-red-300 bg-red-50 px-4 py-2 text-sm text-red-700"
      >
        <span class="font-semibold">Oops!</span> <%= error %>
      </div>
      <% } %>

      <form action="/register" method="POST" class="space-y-4">
        <div>
          <label class="block text-sm font-semibold mb-1">Name</label>
          <input
            type="text"
            name="name"
            required
            value="<%= lastName || '' %>"
            class="w-full border rounded-xl px-4 py-2.5 focus:outline-none focus:ring-2 focus:ring-pink-400"
            placeholder="Your name"
          />
        </div>

        <div>
          <label class="block text-sm font-semibold mb-1">Gift Number</label>
          <input
            type="number"
            name="gift"
            min="1"
            required
            value="<%= lastGift !== null ? lastGift + 1 : 1 %>"
            class="w-full border rounded-xl px-4 py-2.5 focus:outline-none focus:ring-2 focus:ring-pink-400"
            placeholder="e.g. 1"
          />
          <p class="mt-1 text-xs text-gray-500">
            Each gift number must be unique. If you reuse an existing number,
            you’ll get a warning.
          </p>
        </div>

        <button
          type="submit"
          class="w-full bg-pink-500 hover:bg-pink-600 text-white font-bold py-3 rounded-2xl mt-4 transition active:scale-95"
        >
          Save 🎉
        </button>
      </form>

      <div class="mt-6 flex justify-between text-sm">
        <a href="/list" class="text-blue-500 hover:underline">View List</a>
        <a href="/result" class="text-blue-500 hover:underline">Result View</a>
      </div>
    </div>
  </body>
</html>
